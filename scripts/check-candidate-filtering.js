console.log('Checking Candidate Filtering Logic');\nconsole.log('');\n\n// Simulate the current filtering function\nfunction filterValidCandidates(candidates) {\n  const results = {\n    valid: [],\n    rejected: []\n  };\n  \n  candidates.forEach(candidate => {\n    const issues = [];\n    \n    if (!candidate.name || candidate.name.trim() === '') {\n      issues.push('Missing or empty name');\n    }\n    if (!candidate.chestNumber || candidate.chestNumber.trim() === '') {\n      issues.push('Missing or empty chestNumber');\n    }\n    if (!candidate.team || candidate.team.trim() === '') {\n      issues.push('Missing or empty team');\n    }\n    if (!candidate.section || candidate.section.trim() === '') {\n      issues.push('Missing or empty section');\n    }\n    \n    if (issues.length === 0) {\n      results.valid.push(candidate);\n    } else {\n      results.rejected.push({ candidate, issues });\n    }\n  });\n  \n  return results;\n}\n\n// Test with sample data\nconst testCandidates = [\n  { name: 'Ahmed Ali', chestNumber: 'A001', team: 'AQS', section: 'senior' },\n  { name: 'Fatima Hassan', chestNumber: 'B001', team: 'SMD', section: 'junior' },\n  { name: '', chestNumber: 'C001', team: 'MQS', section: 'senior' }, // Empty name\n  { name: 'Omar Khalil', chestNumber: '', team: 'NUR', section: 'junior' }, // Empty chest number\n  { name: 'Sara Ahmed', chestNumber: 'E001', team: '', section: 'sub-junior' }, // Empty team\n  { name: 'Ali Hassan', chestNumber: 'F001', team: 'AQS', section: '' } // Empty section\n];\n\nconst results = filterValidCandidates(testCandidates);\n\nconsole.log('FILTERING RESULTS:');\nconsole.log('Total candidates: ' + testCandidates.length);\nconsole.log('Valid candidates: ' + results.valid.length);\nconsole.log('Rejected candidates: ' + results.rejected.length);\nconsole.log('');\n\nif (results.rejected.length > 0) {\n  console.log('REJECTED CANDIDATES:');\n  results.rejected.forEach((item, index) => {\n    console.log((index + 1) + '. ' + (item.candidate.name || '[NO NAME]') + ' - Issues: ' + item.issues.join(', '));\n  });\n}\n\nconsole.log('');\nconsole.log('EXPLANATION:');\nconsole.log('The filterValidCandidates function is very strict.');\nconsole.log('It filters out ANY candidate that has empty or missing:');\nconsole.log('- name');\nconsole.log('- chestNumber');\nconsole.log('- team');\nconsole.log('- section');\nconsole.log('');\nconsole.log('This could be why you are missing candidates.');\nconsole.log('Check your database for candidates with empty fields.');"